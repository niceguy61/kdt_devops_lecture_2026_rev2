apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: paperclip-routes-wrong-parent
  namespace: week4
spec:
  parentRefs:
    - name: missing-gateway
  hostnames:
    - paperclip.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api
      backendRefs:
        - name: api
          port: 80
