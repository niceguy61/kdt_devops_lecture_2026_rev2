apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: paperclip-no-class
  namespace: week4
spec:
  rules:
    - host: broken.paperclip.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80

